# Bookophilic Dataset Storage

This directory manages static dataset assets, embeddings indexes, and books metadata.

## Folders

- `books_metadata/`: Stores downloaded metadata records.
- `quotes/`: CSV/JSON lists of literary quotes.
- `embeddings/`: Houses local JSON vector databases (`vectors.json`).
- `processed/`: Intermediate processed text chunks.
